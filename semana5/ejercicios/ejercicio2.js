function elevar(num, exponente) {
    // Devuelve el valor de "num" elevado al exponente dado en "exponent"
    // Tu código:
    return Math.pow(num, exponente);
}
const potencia = elevar(2, 3);
console.log(potencia);